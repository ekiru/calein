write line "Hi, what's your name?".

define variable (name)
	with initial value "".

define variable (c)
	with initial value (read character).

while (c) do {
	append character (c) to (name).
	set (c) to (read character).
}.

write "Nice to meet you, ".
write line (name).
